version: "3.8"

services:
  site:
    build: .
    working_dir: /site
    volumes:
      - .:/site
      - bundle:/usr/local/bundle
    ports:
      - "4000:4000"
    command: bundle exec jekyll serve --host 0.0.0.0 --force_polling

volumes:
  bundle:
